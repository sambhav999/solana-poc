OVERFLOW — FINAL DEV HANDOFF
Keep the source. Program the earnings.

This archive is the final code handoff for deployment verification.
No new product scope should be added before the Stocklana submission.

FIRST RUN
1. cp .env.example .env.local
2. Fill all production environment values.
3. npm ci
4. npm run check

DATABASE
Apply migrations in order:
  db/migrations/001_init.sql
  db/migrations/002_integrity.sql
  db/migrations/003_final_verification.sql
  db/migrations/004_dividend_attribution_and_order_binding.sql
Then run:
  npm run verify:db

LIVE ADAPTER VERIFICATION
  npm run verify:xstocks -- MRKx
  npm run verify:kamino -- <REAL_WALLET>
  npm run preflight:prod

MANDATORY BEFORE HACKATHON SUBMISSION
Complete at least one funded end-to-end execution and obtain a VERIFIED_ON_CHAIN receipt.
Put the deployed URL and public Solana transaction signature into README/SUBMISSION materials.

IMPORTANT
- Do not bypass NEEDS_REVIEW/BLOCKED guards.
- Do not change dividend extraction math without re-running tests.
- Do not replace production Supabase with in-memory persistence.
- Replay Mode must remain explicitly labelled historical.
- The build has been statically syntax/import checked in the packaging environment, but npm install/build and funded mainnet execution require the developer's live environment and credentials.
